# Set to true to connect to a live Guppy/TKET/Selene backend
# When false, the UI runs with static demo data
PUBLIC_LIVE_BACKEND=false

# URL of the backend API (used when PUBLIC_LIVE_BACKEND=true)
PUBLIC_BACKEND_URL=http://localhost:8000
